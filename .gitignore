# Environment / secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Data outputs — the deliverable dataset (raw → shortlisted → enriched →
# personalized → tracker) IS committed so reviewers can inspect it. Only the
# large on-disk API caches are ignored; they are regenerable and just bloat.
ass_1/data/.video_cache.json
ass_1/data/.personalize_cache.json
ass_1/data/_backup_*/
ass_1/data/_superseded_*/

# IDE / tooling
.vscode/
.idea/
.claude/
